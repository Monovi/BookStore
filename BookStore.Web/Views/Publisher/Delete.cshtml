@model BookStore.Web.Models.Publishers.PublisherModel

@{
    ViewData["Title"] = $"Delete Publisher: {Model.PublisherName}";
}

<h1>@ViewData["Title"]</h1>

<h3>Are you sure you want to delete this publisher?</h3>

<div>
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Id)
        </dt>
        <dd class="col-sm-10">
            @Model.Id
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PublisherName)
        </dt>
        <dd class="col-sm-10">
            @Model.PublisherName
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PublisherAddress)
        </dt>
        <dd class="col-sm-10">
            @Model.PublisherAddress
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
